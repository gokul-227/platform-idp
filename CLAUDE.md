# Repo guidance for Claude Code

Project conventions live in [`AGENTS.md`](AGENTS.md), the single source of truth shared across agent tools. It is imported below, so every Claude Code session loads it. Add new repo-wide guidance to `AGENTS.md`, not here; user-level preferences belong in `~/.claude/`.

@AGENTS.md
